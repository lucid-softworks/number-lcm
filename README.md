# `@lucid-softworks/number-lcm`

Compute the least common multiple of safe integers.

```ts
import { lcm } from "@lucid-softworks/number-lcm";

lcm(6, 8); // 24
```

See the exported types and tests for exact edge-case behavior.
